function makeShirt(size: string ,message: string) {
    console.log(`My brand's most selling t-shirt is of ${size} size  with the message "${message}" printed on it.`);
}
makeShirt("small","'Free Palestine'")